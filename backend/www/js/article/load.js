;(function($){
	var articleLoader;
	var $el;
	var config;
	
	articleLoader = {
		init: function(cfg){
			alert('not implemented yet.');
			return;
			config = $.extend(config, cfg);
			$el = config.$el;
		}
	};
	
	$.articleLoader = articleLoader;
})(jQuery);